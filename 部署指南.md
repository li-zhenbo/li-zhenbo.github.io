# 个人学术主页部署指南

本指南带你完成网站的上线：GitHub Pages 面向境外访客，Gitee Pages 面向国内访客，两边内容自动同步。整个过程不需要写代码，也不需要服务器，全程免费。

**整体原理**：你的网站源码放在 GitHub 仓库里。每次 `git push` 后，GitHub Actions 会自动把网站构建成静态文件，然后做两件事：发布到 GitHub Pages（境外地址 `你的用户名.github.io`）；同时把构建好的文件推送到 Gitee 仓库，由 Gitee Pages 托管（国内地址 `你的用户名.gitee.io`）。

---

## 第一步：替换占位信息（约 15 分钟）

本站所有需要你填写的位置都带 `TODO-HOMEPAGE` 标记。用编辑器的全局搜索功能搜索 `TODO-HOMEPAGE`，逐个替换即可。主要包括：

| 文件 | 要改什么 |
| --- | --- |
| `_config.yml` | 姓名、`url`（把 `your-github-username` 改成你的 GitHub 用户名） |
| `_data/socials.yml` | 邮箱、GitHub 用户名、Google Scholar ID、Gitee 用户名 |
| `_data/cv.yml` | 简历内容（教育、工作、论文、获奖） |
| `_pages/about.md` | 首页自我介绍（中英两段对照） |
| `_bibliography/papers.bib` | 把两条示例论文换成你的真实论文 |
| `_projects/`、`_posts/`、`_news/` | 示例项目、示例笔记、示例动态 |
| `assets/img/prof_pic.jpg` | 用你的照片覆盖（保持文件名不变） |
| `assets/pdf/example_pdf.pdf` | 用你的 CV PDF 覆盖，并修改 `_data/socials.yml` 里的 `cv_pdf` 路径 |

论文字段的详细说明写在 `_bibliography/papers.bib` 文件头部的注释里。更新各板块的方法也可以参考网站上的《如何更新这个主页》一文（`_posts/2026-07-27-how-to-update.md`）。

## 第二步：GitHub 上线（约 10 分钟）

1. 注册 GitHub 账号（如已有跳过）。假设你的用户名是 `zhangsan`。
2. 在 GitHub 新建一个**公开（Public）**仓库，仓库名必须是 **`zhangsan.github.io`**（即"你的用户名.github.io"），不要勾选 "Add a README"。
3. 在本项目文件夹里执行（Windows 用户可用 Git Bash 或 GitHub Desktop 代替）：

   ```bash
   git init
   git add -A
   git commit -m "初始版本：个人学术主页"
   git branch -M main
   git remote add origin https://github.com/zhangsan/zhangsan.github.io.git
   git push -u origin main
   ```

4. push 后打开仓库的 **Actions** 标签页，会看到 "Deploy site" 正在运行，第一次构建约需 5–10 分钟，等它变成绿色对勾。
5. 打开仓库 **Settings → Pages**，在 "Build and deployment" 下选择 **Deploy from a branch**，分支选 **gh-pages**、目录选 **/(root)**，保存。
6. 稍等一两分钟，访问 `https://zhangsan.github.io` 就能看到网站了。

> 如果 Actions 报错，点进去看红色步骤的日志。最常见的原因是占位信息没改完整（比如 `_config.yml` 里的 `url`），改完再 push 一次会自动重新构建。

## 第三步：Gitee 国内镜像（约 15 分钟）

Gitee Pages 要求**实名认证**，请先在 Gitee 官网（gitee.com）注册账号并完成实名认证（手机号 + 身份证，按页面提示操作即可）。

1. **建仓库**：在 Gitee 新建一个公开仓库。建议仓库名与你的 Gitee 用户名相同（例如用户名是 `zhangsan`，仓库就叫 `zhangsan`），这样国内地址就是最简洁的 `zhangsan.gitee.io`，与 GitHub 侧的根路径结构一致，无需额外配置。
2. **生成部署密钥**（在你电脑终端执行）：

   ```bash
   ssh-keygen -t ed25519 -C "homepage-deploy" -f ~/.ssh/gitee_deploy
   ```

   一路回车（不要设密码）。会得到两个文件：`gitee_deploy`（私钥）和 `gitee_deploy.pub`（公钥）。
3. **把公钥交给 Gitee**：复制 `gitee_deploy.pub` 的内容，粘贴到 Gitee 的 **设置 → 安全设置 → SSH 公钥**。
4. **把私钥交给 GitHub Actions**：打开 GitHub 仓库的 **Settings → Secrets and variables → Actions → New repository secret**，添加三个 secret：

   | 名称 | 内容 |
   | --- | --- |
   | `GITEE_REPO` | `zhangsan/zhangsan`（你的 Gitee 用户名/仓库名） |
   | `GITEE_SSH_PRIVATE_KEY` | `gitee_deploy` 私钥文件的**完整内容**（包括 `-----BEGIN/END-----` 两行） |
   | `GITEE_TOKEN` | Gitee 私人令牌：在 Gitee **设置 → 安全设置 → 私人令牌** 生成，权限勾选 `projects` 即可 |

5. **开启 Gitee Pages**：进入 Gitee 仓库 → **服务 → Gitee Pages**，部署分支选择 **gh-pages**，点击"启动/更新"。（第一次需要 push 一次代码触发 GitHub Actions 把 gh-pages 分支推过去之后，这里才能选到该分支；也可以先手动在 Gitee 创建一个 gh-pages 分支占位。）
6. 之后再 push 代码，GitHub Actions 构建完成后会自动同步到 Gitee 并触发 Gitee Pages 更新。访问 `https://zhangsan.gitee.io` 验证。

> **注意**：Gitee 免费版偶尔会出现 API 触发更新失败的情况。如果 GitHub Actions 日志里出现黄色警告 "Gitee Pages 自动更新触发失败"，到 Gitee 仓库的"服务 → Gitee Pages"页面手动点一下"更新"即可，内容已经同步过去了。三个 secret 任何一个没配置，同步步骤都会自动跳过，不影响 GitHub 侧的正常发布。

## 第四步：验证与日常使用

- 境外地址 `https://你的用户名.github.io`，国内地址 `https://你的用户名.gitee.io`，建议都打开检查一遍。
- 日常更新只需要改文件然后 `git add -A && git commit -m "更新" && git push`，两边都会在几分钟后自动更新。
- 简历 PDF、照片、论文 PDF 等静态文件分别放在 `assets/pdf/` 和 `assets/img/` 下。
- 如果你在论文里标记了 `google_scholar_id`，仓库里自带的 "Update Google Scholar Citations" 工作流会每周一、三、五自动抓取引用数并显示在论文条目下方（首次配置后无需任何操作）。

## 本地预览（可选，非必需）

想在电脑上先预览效果再发布，二选一：

- **Docker（推荐，最省事）**：安装 Docker Desktop 后，在项目文件夹执行 `docker compose up`，浏览器打开 `http://localhost:8080`。
- **Ruby 环境**：安装 Ruby 3.3 和 ImageMagick 后执行 `bundle install && bundle exec jekyll serve`，打开 `http://localhost:4000`。

不预览也完全没问题——直接在 GitHub 网页上编辑文件提交，同样会触发自动部署。

## 常见问题

**Q：GitHub 在国内访问慢，管理和 push 代码不方便怎么办？**
只有"管理代码"这一步需要连 GitHub，访客访问的是 Gitee 镜像，不受影响。如果 push 困难，可以在 Gitee 网页端直接导入 GitHub 仓库做中转，或用 GitHub 网页编辑器直接改文件（打开仓库里任意文件点铅笔图标），提交后同样触发自动部署。

**Q：能不能两边用同一个域名？**
可以，需要买一个域名（约 ¥70/年）并做分线路 DNS 解析（境内解析到 Gitee、境外解析到 GitHub Pages）。但注意：国内平台绑定自定义域名要求域名完成 ICP 备案，不想备案的话就维持现有的两个 `.github.io` / `.gitee.io` 地址，这是最省心的状态。

**Q：想要评论功能？**
本站已内置 giscus（基于 GitHub Discussions）的支持，按 https://giscus.app/ 的指引填写 `_config.yml` 里的 `giscus:` 段即可。国内访客使用 giscus 需要能访问 GitHub，请按需开启。

**Q：如何隐藏某个板块？**
不想用博客就把 `_pages/blog.md` 里 front matter 的 `nav: true` 删掉（或改为 `nav: false`），并删除 `_posts/` 里的示例文章；论文、项目、CV 同理，对应文件都在 `_pages/` 目录。

**Q：Gitee Pages 提示需要付费或审核？**
Gitee 的政策时有调整：免费版 Pages 需要实名认证，自动部署等高级能力可能需要 Pro。本方案中 GitHub Actions 已经把内容推送到位，即使手动点"更新"也就是每次发布多点一下鼠标的事。若未来 Gitee Pages 政策变化，可随时把国内侧迁移到腾讯云静态网站托管等服务，网站源码不用做任何改动。

## 需要帮助时

- 本站主题（al-folio）的官方文档：https://github.com/alshedivat/al-folio
- 主题自带详细文档在 `docs/` 目录已删除以精简仓库，可在线查阅：INSTALL.md（安装）、CUSTOMIZE.md（自定义）、FAQ.md（常见问题）
